apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: helloworld-app-placement
  namespace: multi-placement-ns
spec:
  clusterReplicas: 1
  clusterSelector:
    matchLabels:
      environment: Dev
